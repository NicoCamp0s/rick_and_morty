

const initialState = {
    myFavorites: [],
}

const rootReducer = (state = initialState, action) => {
    switch(action.type) {
        case myFavorites:
            
            
        case deleteMyiFavorites:


        default:
            return {...state};
    }
}